# Alberto Fasulo Portfolio

Interactive personal portfolio for technical engineering applications.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## GitHub Pages

The site is configured for GitHub Pages with the repository path `/personal_website/`.
If the repository is renamed, update `base` in `vite.config.ts`.
